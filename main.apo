struct Person {
  name: string
  age: i4
}
fn new_person(name: string, age: i4) -> Person {
  return new Person {
    name: name
    age: age
  }
}
let lucas = new_person("Lucas Henrique Santana Marcelino", 19)
printf("%s is %d years old", lucas.name, lucas.age)
return 0
