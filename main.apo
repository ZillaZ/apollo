use std { threads::threads { fork sleep } }

fn hello(name: string) {
	printf("Hello, %s!\n", name)
	return
}

let args = -1
let name = "Lucas"
hello(name)
threads::fork!(||{
	let name = "Child"
	hello(name)
	return
})
