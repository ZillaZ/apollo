trait Iterable {
  length: i4
  elements: array(i4)
}

struct Vec {
  #[Iterable]
}

let iterable = new Vec {
  length: 100
  elements: array(i4 100)[]
}

fn square(number: i4) -> i4 {
  return number * number
}

for i in (0..iterable.length) {
  iterable.elements[i] = square(i+1)
}

for i in iterable {
  printf("%d ", i)
}


printf("out of loop")
