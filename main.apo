use static array
let array = new_array_with_capacity(100)
let helper = new Helper {
  value: 99
}
array.push(helper)
printf("%d %d %d", array.contains(53), array.contains(99), array.contains(9))
return 0
